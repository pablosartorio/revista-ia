"""Cóndor — CMS, checkpoint humano y validadores de la redacción multi-agente."""

from pathlib import Path

RAIZ = Path(__file__).resolve().parent.parent
DATA = RAIZ / "data"
OUTPUT = RAIZ / "output"
